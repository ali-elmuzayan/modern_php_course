<?php

// what is alpine js
/*
 * it is a lightweight, javascript, framework
 *
 * Note:
 *  - it is used together with tools like livewire so no need to add it
 *    when using livewire
 *  - it is a way to avoid using front end frameworks like react and vue
 *
 *
 * to add alpine
 *  - <script src="//unpkg.com/alpinejs" defer></script>
 *
 */

// to hide element
/*
 * add
 * - x-data={{ name: true }} to the element
 *
 * and add event to th element
 *  - so when you click on it the other dive with data name will be flase
 *    => @click="flash = false"
